server.port=${SERVER_PORT:8081}
spring.datasource.url=${DATABASE_URL:jdbc:mysql://db4free.net:3306/spotify_123}
spring.datasource.username=${DATABASE_USERNAME:daotq_1}
spring.datasource.password =${DATABASE_PASSWORD:Boyalone123}
max_allowed_packet=900M
#spring.jpa.hibernate.ddl-auto = create
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true
spring.jpa.properties.hibernate.format_sql=true
spring.data.web.pageable.one-indexed-parameters=true
spring.jpa.properties.hibernate.event.merge.entity_copy_observer=allow
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB
## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=download
